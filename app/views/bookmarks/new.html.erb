
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all %>
  <%= f.submit %>
<% end %>


<%# <%= link_to "Delete",
  list_bookmarks_path(@bookmark),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
